import { StyleSheet } from 'react-native';

export const getCustomSummaryStyles = () => {
  return StyleSheet.create({
    historySectionContainer: {
      justifyContent: 'center',
      alignItems: 'center',
      gap: 10,
    },
  });
};
